export type TFilterData = {
  name: string;
  value: string;
  isChecked: boolean;
};

export type TProfileData = {
  gender: string,
  age: string, 
  hairColor: string, 
  height: string,
  name: string,
  photo: string,
};